# Deployment

Production alias: `https://requirements-impact-analyzer-shunsoco-stacks-projects.vercel.app/`

The deployment currently requires Vercel Authentication because project or team deployment protection is enabled. Disable Vercel Authentication for Production in the project security settings to make the alias publicly accessible.
